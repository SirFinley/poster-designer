{
  "name": "api-test-upload-image",
  "version": "1.0.0",
  "main": "bin/index",
  "author": "",
  "license": "ISC",
  "description": "",
  "files": [
    "bin/**/*.*",
    "package.json"
  ],
  "scripts": {
    "update": "tsc && claudia update --api-module bin/index --region us-east-1",
    "create": "tsc && claudia create --api-module bin/index --region us-east-1",
    "pack": "tsc && claudia pack --api-module bin/index --force"
  },
  "dependencies": {
    "claudia-api-builder": "^4.1.2"
  }
}
