{
  "name": "save-poster",
  "version": "1.0.0",
  "main": "bin/index",
  "author": "",
  "license": "ISC",
  "description": "",
  "files": [
    "bin/**/*.*",
    "package.json"
  ],
  "scripts": {
    "update-api-mapping": "node ../etc/update-api-mapping.js",
    "update": "tsc && claudia update --no-optional-dependencies --api-module bin/index --region us-east-1 --policies policies/*.json && npm run update-api-mapping",
    "create": "tsc && claudia create --no-optional-dependencies --api-module bin/index --region us-east-1 --policies policies/*.json && npm run update-api-mapping",
    "pack": "tsc && claudia pack --no-optional-dependencies --api-module bin/index --force"
  },
  "dependencies": {
    "claudia-api-builder": "^4.1.2",
    "hi-base32": "^0.5.1"
  },
  "optionalDependencies": {
    "aws-sdk": "^2.1027.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.7"
  }
}
